import api from './api.js';


const mdLinks = (path, options) => {
    return new Promise((resolve, reject) => {
        const rutaExiste = api.rutaExiste(ruta);
        if (rutaExiste === false) {
            reject("La ruta no es válida")
        } else {
          const convRutaAbsol
            

export default { mdLinks }